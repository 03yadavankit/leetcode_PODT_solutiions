class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int mx=0,res=0;
        for(int i=0;i<gain.size();i++)
        {
            res = res+gain[i];
            mx = max(mx,res);
        }
        return mx;
    }
};
